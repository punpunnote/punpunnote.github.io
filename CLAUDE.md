# ぷんぷんノート — 運用ルール

GitHub Pages（Jekyll標準ビルド、追加ツール不要）+ A8.netアフィリエイトの副業ブログ。
`main`ブランチへのpushで自動的にビルド・公開される。ローカルにHugo/Node/Jekyll等のインストールは不要。

> **この文書の構成**: 「A. 恒久ルール」はジャンルが変わっても不変。
> 「B. 現在のジャンル戦略」はジャンル転換時に差し替える対象。両者を混ぜないこと。

---

# A. 恒久ルール（ジャンルに依存しない）

## サイトはジャンル中立に保つ
「ぷんぷんノート」はジャンルを名前に含めない汎用ブログとして設計されている。
将来どの分野へ転換しても、以下は書き換えずに済むようにしておくこと。

- `_config.yml` の `title` / `description` にジャンル固有の語を入れない
- `about.md` にジャンル固有の説明を書かない
- ジャンルは**カテゴリで表現する**。サイト設定やURLでは表現しない
- `permalink: /:title/` を維持する。カテゴリをURLに含めると、記事を再分類した際に
  URLが変わり、被リンクと検索評価を失う。**この設定は変更しないこと**
- カテゴリ一覧は `categories.md` がLiquidで自動生成する。新ジャンルを足しても編集不要

## ジャンルを転換・追加するときの手順
1. 新しい `categories:` を使って `_drafts/` に記事を書き始める（既存記事は触らない）
2. 本文書の「B. 現在のジャンル戦略」節を差し替える
3. A8.netのサイト情報でジャンル追加が必要なら申請する
4. **既存記事は消さない**。URLが変わらない設計なので、放置しても損害はない

近いジャンルならカテゴリ追加で足りる。まったく無関係なジャンルを本格運用する場合のみ、
独自ドメイン＋別リポジトリでサイトごと分けることを検討する
（別リポジトリを作るだけでは `punpunnote.github.io/リポジトリ名/` となり、同一サイト扱いで分離されない）。

---

# B. 現在のジャンル戦略（転換時に差し替える）

**契約の見直し・乗り換え手続き（通信・電力・サブスク）**

- **誰に**: 契約の乗り換え・解約を検討している人。料金改定、新プラン登場、更新月、
  引っ越しなど、きっかけは個人ごとにバラバラで、需要は通年ほぼ平坦。
- **何を**: 手順と事実で答えが決まる疑問への回答。解約のタイミング、必要な手続きの順番、
  日割りの扱い、乗り換え時に起きる空白期間など。
- **なぜこの型か**: 書き手に実体験がない前提のため、答えが手順と事実で決まるクエリに
  限定する。「おすすめ◯選」型は体験が必須で競合に勝てず、量産判定のリスクも高いので扱わない。

### 季節性について（重要）
当初は「新生活・引っ越し」を軸にしたが、需要が1〜3月に集中し年9ヶ月死ぬこと、
最も競争の激しい時期に最も弱いドメインで参入することになるため、一段抽象化した。
引っ越しは「住み替えに伴う契約変更」として本カテゴリに内包されるので、
新生活向けの記事は3月期向けの一部門として書いてよい。ただし軸には据えない。

### 記事の構成
- **ハブ記事**: あるサービス分野の乗り換え全体像。回遊の起点。
- **スポーク記事**: 個別の具体的な疑問に答える。内部リンクでハブと相互に結ぶ。
- 収益導線の中心はネット回線・電力（乗り換え時の手続きが複雑なため、
  正確な手順の解説そのものが読者の実利になり、訴求と一致する）。
- 全記事を収益記事にしない。手続き解説に徹した収益ゼロの記事を一定割合で保ち、
  信頼構築と内部リンクを担わせる。

### この戦略の上限（認識しておくこと）
成果報酬型のため読者にリピーターは付かない。「検索で見つかり、一度読まれ、成約する」
までが上限であり、読者資産は蓄積しない。蓄積型にするには書き手の体験・人格が必要だが、
今回はそれを使わない方針のため、この上限は受け入れた上で運用する。

---

# C. 記事作成と公開（ジャンルに依存しない）

## 記事の作成ルール
- 新規記事はまず `_drafts/slug.md` に書く（`templates/post_template.md`を土台にする）
- slugはローマ字。日本語を使わない（URLに入るため）
- フロントマターは `layout: post` / `title` / `categories` を必須とする（`date`は公開時に付与）
- PR表記は `_layouts/post.html` に組み込み済みなので、本文中に重ねて書く必要はない
- 断定的な数値（価格・スペック・スケジュール・合格率など）は執筆時点で古くなっている前提で扱い、
  一次情報が必要な箇所は本文で断定せず公式サイトへリンクする
- 実際に使用していない商品について体験談的な表現（「使ってみた」「実際に使用して」等）は書かない
- 健康・医療・法律・投資助言など、誤情報が実害に直結する領域の断定的な記述はしない

## 公開フロー（下書き方式）

`_drafts/` はJekyllがビルド対象から除外する標準フォルダで、ここに置いた記事はpushしても
サイトに公開されない。これを利用して「生成は全自動・公開だけ人間が判断」を実現する。

1. **生成（自動）**: 記事を `_drafts/slug.md` に作成し、コミット＆push。
   サイトには一切出ないので、この段階でのpushに人間の承認は不要。
2. **レビュー（自動）**: サブエージェントによる査読を行う。詳細は下記。
3. **修正（自動）**: レビューの指摘を反映し、コミット＆push。
4. **確認（人間）**: 利用者が下書きを読み、公開するものを選ぶ。
5. **公開（承認後）**: `_drafts/slug.md` を `_posts/YYYY-MM-DD-slug.md` へ移動し、
   フロントマターに `date` を追加してコミット＆push。ここで初めてサイトに出る。

**このステップ5は不可逆な公開行為にあたるため、利用者の明示的な指示があった場合のみ実行すること。**
下書きの生成・レビュー・修正を理由に、公開まで続けて行ってはならない。

### レビュー（ステップ2）の実施方法

**利用者に公開判断を求める前に、必ず実施すること。** 未レビューの下書きを
そのまま人間の確認に回してはならない。レビュー負荷を利用者に転嫁しないための工程である。

サブエージェントを3体、並列で起動する。各エージェントには対象ファイルの絶対パスを渡す。

| 観点 | 検出対象 |
|---|---|
| **事実・正確性** | ハルシネーション、断定しすぎ、手順の誤り、検証不能な記述 |
| **規約・法務** | 景表法・ステマ規制、誇大表現、YMYL領域への踏み込み、A8審査で不利になる要素 |
| **品質・SEO** | 量産コンテンツ的な兆候、内容の薄さ、構成の重複、読者価値 |

オーケストレーター役のサブエージェントは挟まない。過去2回、配下エージェントの結果は
届いたが統括エージェント自身の集約が返らなかったため、**3体を直接並列起動し、
集約と修正の適用は本体が行う**こと。

指摘は「ファイル名＋該当箇所＋具体的な問題」の形で返させる。
「全体的に良い」といった評価は不要である旨を明示する。
